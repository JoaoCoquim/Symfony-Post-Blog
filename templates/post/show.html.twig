{% extends 'base.html.twig' %}

{% block body %}
    <h1>You are looking at post {{ post.id }}:</h1>
    <h2>{{ post.title }}</h2>
    <img src="{{ post.image }}" alt="{{ post.title }}">
{% endblock %}